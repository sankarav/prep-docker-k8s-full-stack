apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: full-stack
    app-module: postgres-db
spec:
  selector:
    app: full-stack
    app-module: postgres-db
  ports:
    - port: 5432
      targetPort: 5432